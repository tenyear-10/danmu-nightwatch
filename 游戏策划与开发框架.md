# 《弹幕守夜城》：求职作品策划与开发框架

当前状态：本策划已实现为 v0.3.1 可运行 Demo，入口、实际功能和启动方式见 README.md。下文保留最初的设计基线；实际平衡改动以 src/config/balance.ts 和验证记录.md 为准。

## 1. 项目定位

一句话：观众用弹幕部署自己的炮塔、调整防线、投票升级，在三分钟内共同守住一座蒸汽朋克小城。

它是合作塔防，能与已有的坦克大战形成玩法差异。作品用于证明：能够完成游戏循环、把互动事件变成游戏行为、用 AI 辅助制作和调试，并能说明代码与设计选择。

默认目标：PC 浏览器运行、单机模拟直播互动、16:9 游戏画面、中文界面。首版不依赖账号、服务器、付费服务或大模型在线响应。截图中的岗位描述只用于判断作品方向，不是接入或上线要求。

## 2. 核心体验

观众看到怪物逼近城门 → 输入“加入 火炮” → 带昵称的炮塔落到场上 → 自动开火并显示贡献 → 发“下路”支援薄弱防线 → 全体投票选升级 → 合力击败 Boss → 结算、重新开始。

三个需要被看见的反馈：

- 身份：炮塔上方显示经过截断处理的昵称，加入时高亮两秒。
- 影响：换路改变攻击目标；减速有颜色反馈；升级即时改变战斗效果。
- 集体时刻：Boss 登场、城门低血量警报、全体技能触发、胜利结算。

## 3. 战场和操作

城门在左，敌人从右进入；上、中、下三条水平路线。每条路线有四个炮塔槽位，总共十二个。

炮塔固定在路线的防御区域，自动攻击射程内、距离城门最近的敌人。敌人抵达城门后扣除耐久并移除；炮塔首版不承受伤害。无需寻路、物理碰撞或角色复活系统。

| 输入 | 行为 | 限制与反馈 |
| --- | --- | --- |
| 加入 火炮 | 创建单体输出炮塔 | 每个 viewerId 每局最多一个；默认分配人数最少的路线，平局上、中、下依次优先 |
| 加入 冰塔 | 创建减速炮塔 | 同上；重复加入不新增，也不改变职业 |
| 加入 雷塔 | 创建群体输出炮塔 | 同上 |
| 上路 / 中路 / 下路 | 移动自己的炮塔到目标路线空位 | 成功后冷却五秒；目标满员则保持原位并提示 |
| 助威 | 增加一点全体能量 | 每位观众三秒冷却；炮塔满员后的观众也能参加 |
| 1 / 2 / 3 | 在升级投票开启时投票 | 每人每轮一票；截止前允许改票 |

十二个炮塔满员后，新的“加入”请求提示“炮位已满，可助威或投票”，不悄悄生成不可见单位。不实现候补、替补和挂机踢出。

普通聊天不执行动作。首版仅识别上述明确指令，先 trim 并统一连续空白，不用大模型解释弹幕。

所有观众身份按 viewerId 识别，昵称只用于显示。模拟面板允许切换身份；自动生成的身份明确标记为演示账号。

## 4. 一局的时间线

- 标题页：玩法说明、“开始演示”和“手动测试”两个入口。
- 开始演示：添加火炮、冰塔、雷塔各一名演示队员，分别进入三条路；定时注入有标识的模拟弹幕。
- 手动测试：不添加队员，由面板发送事件；没有队员时保持准备状态。
- 第一名队员加入后，主播点击开始，五秒倒计时；倒计时后进入战斗，记作 t=0。
- t=0、30、60、90、120、150 秒：生成六波敌人。第六波包含中路 Boss。
- t=60、120 秒：分别开启八秒升级投票，战斗继续；关闭时应用获胜升级。
- 第六波已生成且场上全部敌人被消灭：胜利。
- 城门耐久归零：失败。t=180 秒仍有敌人：超时失败。
- 结算停留，用户点击“再来一局”回到准备状态，保留选择的演示模式但重建本局数据。

同一逻辑帧先处理攻击和抵达城门，再判断：城门毁坏优先于胜利，胜利优先于超时。恰好在截止帧清空敌人且城门存活算胜利。

Boss 不新增第四条路或特殊寻路；用大体型、高耐久和周期加速制造压迫感。前两波仅普通怪，第三波开始加入快怪，第五波加入重甲怪。

## 5. 初始数值：仅用于首次试玩，不能当成已验证平衡

| 内容 | 初值 |
| --- | --- |
| 设计画布 | 1280 × 720，等比缩放，逻辑距离按设计像素计算 |
| 战斗逻辑 | 固定 30 tick/s；渲染独立更新 |
| 城门耐久 | 100 |
| 火炮 | 16 伤害，1 秒攻击间隔，450 射程，单体 |
| 冰塔 | 5 伤害，1.2 秒间隔，450 射程；减速 30%，持续 2 秒 |
| 雷塔 | 每目标 6 伤害，1.5 秒间隔，450 射程；同路最多命中 3 个目标 |
| 普通怪 | 45 耐久，24 像素/秒，抵达城门扣 4 耐久 |
| 快怪 | 25 耐久，42 像素/秒，扣 3 耐久 |
| 重甲怪 | 150 耐久，15 像素/秒，扣 10 耐久；首版不加护甲减伤公式 |
| Boss | 900 基础耐久，10 像素/秒，抵达扣 100 耐久；每 10 秒加速 50%，持续 2 秒 |
| 全体技能 | 30 能量自动释放雷暴，对全场敌人造成 35 伤害 |

雷塔不是物理链式弹射，只选择射程内三个目标并播放连线。冰塔减速不叠乘，重复命中刷新到期时间。雷暴能量上限 30，触发后归零，全体技能有五秒冷却；冷却期间可积攒到 30，结束后触发。

每波基础怪物总数为 [6, 9, 12, 15, 18, 12]，第六波另外生成一个 Boss。普通波平均分配三路，余数按上、中、下分配；一秒间隔依次入场，避免同一帧堆叠。

波次生成时记录炮塔数 N：怪物耐久倍率 = clamp(sqrt(N / 3), 0.75, 2)。本波生成队列固定使用该倍率；中途加入不修改已生成敌人。人数不增加怪物数量，避免大人数导致实体失控。这个公式只是起点，需要分别试玩 1、3、6、12 个炮塔后调整。

每次升级固定提供三选一：1 全队伤害 +15%；2 城门恢复 20（不超过上限）；3 攻击间隔 ×0.9。两次升级可重复选择，数值乘法叠加；无人投票或平票按选项编号较小者选定。投票升级只影响未来攻击。

结算显示团队结果、存活时间、总击杀、个人有效伤害前三名、助威次数前三名。有效伤害不能超过目标剩余耐久；雷暴计入团队伤害，不归给最后一位助威者。

## 6. 画面布局与美术范围

顶部：城门耐久、波数、倒计时、全体能量条。

中部：占画面约 70% 的三路战场，暖色城门、冷色夜景、橙色炮火。高对比轮廓优先于复杂细节，昵称不能盖住血条。

底部：四条简短玩法指令、最新互动反馈、投票卡。右侧可折叠开发面板，不在游戏画面中放实现术语。

首版先用几何图形完成玩法；美术阶段替换为一张背景、三种炮塔、三种普通敌人、一个 Boss、弹丸与命中特效。统一斜俯视或侧视角，不混用透视。先用静态精灵加轻微位移、缩放、闪白，避免一开始承担骨骼动画成本。

音效只需加入、射击、命中、升级、警报、胜败六类；提供静音。资源保存来源与许可记录。若使用 AI 生成，保存提示词和人工修改记录。

## 7. 技术选型

建议 TypeScript + Phaser + Vite。用 Phaser 负责画布、场景、精灵、声音，用普通 HTML/CSS 实现开发面板。Phaser 官方提供 TypeScript/Vite 项目模板：

- https://docs.phaser.io/phaser/getting-started/project-templates
- https://phaser.io/news/2024/01/phaser-vite-typescript-template
- https://docs.phaser.io/phaser/concepts/scenes

在开始实现时从官方模板核对并固定实际依赖版本，提交 lockfile。本方案不指定未经验证的版本号。

无需先加 React、数据库、登录、多房间或联网多人状态同步。这里是一个集中运行的游戏实例接受许多互动事件，不是每个观众运行一个联网客户端。

## 8. 代码框架：事件、规则、显示三层

```text
模拟面板 / 自动模拟器 / 未来真实事件适配器
                    ↓ 统一事件
              校验、去重、限流
                    ↓
                弹幕指令解析
                    ↓
          有界队列 → 固定步长游戏逻辑
                    ↓
          游戏状态 → Phaser 画面 / DOM 面板
```

只有游戏逻辑层能修改血量、炮塔、波次、能量和结算数据。Phaser 场景读取状态并显示效果，不承担第二份战斗计算。弹丸首版是显示效果，命中与伤害由逻辑层决定。

建议目录；以下是待实现结构，不代表文件已经存在：

```text
src/
  main.ts
  config/balance.ts          # 炮塔、怪物、波次、节奏和容量
  core/types.ts             # 统一事件、指令、状态与实体类型
  core/GameSession.ts       # 状态机、固定步进、各系统调用顺序
  core/combat.ts            # 目标选择、攻击、减速、有效伤害
  core/waves.ts             # 波次调度、入场队列、人数快照
  core/voting.ts            # 开关投票、改票、计票、应用升级
  input/commands.ts         # 纯函数：弹幕转指令
  input/EventGateway.ts     # 校验、去重、冷却、有界队列
  input/MockAdapter.ts      # 手动注入和自动模拟，可开始/停止
  scenes/BootScene.ts       # 加载资源
  scenes/BattleScene.ts     # 游戏渲染；准备和结算可用覆盖层
  ui/DebugPanel.ts          # 注入事件、暂停、重开、压力测试
  ui/style.css
tests/
  commands.test.ts
  events.test.ts
  session.test.ts
public/assets/
  ASSET_SOURCES.md
README.md
```

先让 GameSession 协调少量纯函数，不创建 ECS、依赖注入容器或通用插件系统。文件过短可合并，目录是责任划分，不是必须凑齐的文件清单。

统一事件契约：

```ts
type InteractionEvent = {
  eventId: string;          // 本局内用于防止重复执行
  sessionId: string;        // 防止上一局迟到事件进入新局
  viewerId: string;         // 稳定身份，不使用昵称代替
  displayName: string;
  receivedAtMs: number;     // 本机接收时刻，仅用于诊断
  source: 'mock' | 'platform';
  payload: { type: 'chat'; text: string };
};

type Phase = 'lobby' | 'countdown' | 'playing' | 'result';
type Lane = 0 | 1 | 2;
type TowerKind = 'cannon' | 'ice' | 'tesla';

interface EventAdapter {
  start(emit: (event: InteractionEvent) => void): void;
  stop(): void;
}
```

限流、投票截止和技能冷却都使用游戏逻辑时钟，不能信任事件携带的时刻。暂停时停止游戏逻辑时间并拒绝新的动作事件，提示暂停；恢复时不补跑后台停留期间的战斗。

一帧的顺序固定：消费事件 → 开启到点波次/投票 → 关闭到期投票 → 更新状态效果 → 炮塔攻击 → 全体技能 → 移动敌人并扣城门耐久 → 判定结果。投票接受条件显式检查 startTick <= currentTick < endTick，避免截止帧多收一票。需要随机数时由本局 seed 产生，不在核心规则里直接调用 Math.random。

## 9. 弹幕量与重开问题

- 本局最多记录 10,000 个已见 eventId，超额按最早记录淘汰；因此它是有界去重窗口，不承诺无限期去重。
- 待处理事件队列最多 500 条，每 tick 最多处理 20 条。队列满则拒绝新事件并增加可见计数。
- 同一 viewerId 的输入最多每秒五条，成功换路和助威还需通过各自冷却；这些参数集中配置。
- 昵称最多显示 12 个 Unicode 字符，聊天输入长度上限 80；DOM 使用 textContent，不能用昵称拼 innerHTML。
- 互动反馈只保留最近 50 条；投票和观众数据每局有上限，演示压力测试最多 200 个身份。
- 全场敌人上限 120，满时待生成怪物留在生成队列；胜利判定必须同时确认生成队列为空。
- 再来一局时停止旧适配器、解除旧监听、清空队列/去重/投票/冷却/实体，产生新 sessionId，再开始所选模式。
- 首版不做断线追赶。未来平台断开时显示状态并继续现有战斗，禁止用模拟事件冒充真实输入。

## 10. 真实接入边界

本地演示必须显示“模拟互动”，不能声称已经接入抖音。

后续只有在拿到实际可用的官方或授权接口资料、授权条件及事件样本后，才实现真实适配器。这里没有核实某个具体平台接口的开放范围，也不假设存在某条 WebSocket 地址。

如果实际接口需要凭证或服务端签名，再增加服务端适配服务，凭证不能进入前端包。点赞、礼物事件是后续需求，确认事件含义和重复/累计推送语义之后再设计映射；当前 MVP 不把付费礼物设为胜利条件。

## 11. 实施顺序与完成标准

| 阶段 | 交付内容 | 验证方式 |
| --- | --- | --- |
| A 可玩循环 | 几何图形、三路、单种塔与怪、波次、城门、胜败、重开 | 无网络也能完整完成一局；胜利与失败均可触发 |
| B 互动闭环 | 统一事件、加入、换路、助威、模拟面板 | 手动发送指令可见改变；重复事件不重复执行 |
| C 完整玩法 | 三种塔、三种怪、Boss、升级投票、贡献榜 | 三种职业效果不同；投票影响数值；结果统计正确 |
| D 演示质量 | 统一资源、音效、警报、教程、压力测试 | 录制一段两分钟视频，展示核心互动与结算 |
| E 交付整理 | README、构建包、已知限制、AI 开发记录 | 新环境按说明能启动，构建和关键测试通过 |

若时间不够，先砍美术动画、自动模拟的花样、复杂统计；保留加入→战斗→影响结果→结算→重开的闭环。

关键自动测试覆盖：重复事件；一人重复加入；满员处理；换路冷却与满路；投票改票和截止；减速不叠乘；有效伤害不溢出；边界帧胜败顺序；旧 session 事件；重开清理。不要为纯装饰样式堆测试。

手动验证：1/3/6/12 炮塔各试玩；200 个模拟身份、每秒 50 条消息持续两分钟；重复重开十次；切到后台再回来。记录机器、浏览器、实际帧率和事件拒绝数，不写未测过的性能结论。

平衡目标：三名演示队员加适度互动可以取胜，但挂机时会出现可见压力；一条路没人时，该路敌人应造成损失，鼓励换防。如果结果不符，调整数值与波次，不暗中给演示模式加无敌。

## 12. 展示时怎样证明 AI 开发能力

保留三条真实记录即可：

1. 一次从需求到规则和代码的过程，说明你决定了什么、AI 生成了什么。
2. 一个实际 Bug 的复现、定位、修复和回归结果，例如重复事件导致重复生成炮塔。
3. 一次素材或数值迭代，展示前后差异与选择理由。

不需要在游戏里强行塞聊天机器人，也不能编造开发记录。这个岗位截图强调的是用 AI 完成开发工作，作品应让人看到你能约束、理解、验证生成结果。

建议两分钟录像顺序：10 秒介绍；20 秒三个身份加入；20 秒换路和技能；20 秒投票升级；30 秒 Boss 和结算；20 秒展示模拟面板、一次真实测试结果与架构。

完成后可据实介绍：“我做了一个三路合作塔防 Demo，支持弹幕加入、换防、助威和升级投票，具备完整战斗与结算流程。当前用模拟互动演示，真实平台接入留有独立适配层。”未完成的功能从介绍中删掉。
