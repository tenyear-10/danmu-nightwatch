# 验证记录

日期：2026-09-19 初次验证，2026-09-20 继续修复和复测，2026-09-24 检查 v0.3 界面，2026-09-25 修复弹幕召唤。以下仅记录实际运行的检查。

## 环境

- Windows 桌面环境。
- Node.js 24.19.0，TypeScript 5.9.3，Vite 7.3.6，Vitest 3.2.7，Phaser 3.90.0。
- 浏览器：Codex 内置浏览器。桌面布局验证尺寸 1440 × 1000。

## 自动检查

`node node_modules/typescript/bin/tsc --noEmit`：通过。

`node node_modules/vitest/vitest.mjs run`：30 项通过（2026-09-25 最后运行）。

覆盖：指令白名单与对象原型边界、输入校验、去重、过期局事件、限流、队列上限、相同昵称不同身份、重复加入、炮位上限、满路换防、冷却、暂停、Unicode 昵称、投票改票/截止/平票、耐久恢复上限、伤害统计、雷暴归属、减速、群攻上限、生成队列、胜败优先级、波次快照、整局和重开。

压力回归：200 身份，每秒 50 事件，模拟 120 秒，共 6,000 条；6,000 条全部处理，队列溢出 0，最终排队 0，观众 200，日志不超过 50，效果不超过 200。此项是逻辑层加速模拟，不是浏览器实时两分钟测试。

重开回归：连续建立十个新局，旧适配器停止后不再发事件，旧 sessionId 被新局拒绝，新局队列、炮塔和升级为空。

新增回归：在自动观众尚未全部发言时开始压测，填满 200 身份，再恢复自动输入二十秒。最末一个助威身份正常继续，容量拒绝为 0；重复生成压测名单仍为 200 个互不重复的身份。

v0.3.1 回归：测试先复现“召唤火炮”被当作普通文本、没有部署炮塔；实现后，明确的“召唤/加入 + 兵种”指令可部署，普通聊天仍不触发战斗操作。

## v0.3.1 弹幕召唤浏览器检查（2026-09-25）

- 在正式构建包的页面输入框发送“召唤火炮”：先显示“指令已送达，等待战场确认”，随后显示“火炮已部署到中路”；个人状态改为“火炮 · 中路”，炮位从 3/12 变为 4/12。
- 同一页面发送“今晚天气不错”：显示“普通弹幕，不会触发操作”，炮位保持 4/12。
- 浏览器 warning/error 日志为空；此处检查的是页面内模拟弹幕，不代表真实直播平台接口已接通。

## 数值模拟结果

| 场景 | 结果 | 城门耐久 | 击杀 | 结束时间 |
| --- | --- | --- | --- | --- |
| 默认三炮塔＋自动助威/投票/换防 | 胜利 | 100 | 73 | 179.53 秒 |
| 1 炮塔，无助威无换防 | 城门失守 | 0 | 17 | 144.57 秒 |
| 3 炮塔，无助威无换防 | 超时 | 94 | 67 | 180 秒 |
| 6 炮塔，无助威无换防 | 超时 | 100 | 71 | 180 秒 |
| 12 炮塔，无助威无换防 | 胜利 | 100 | 73 | 179.63 秒 |

数值是 v0.2 起的固定配置结果。部署会优先选择人数较少且同职业较少的路线；人数较少时，首领阶段仍需要换防或助威。数据不代表所有人数/职业组合的胜率。

## v0.3 界面检查（2026-09-24）

- 静态构建包在本地浏览器打开，桌面宽度下检查开局、第一波、部署火炮后的选中牌与换防按钮。控制项正常更新，战场未被新样式遮挡。
- 390 × 844 窄屏浏览器检查：文档宽度 375（含滚动条），无横向溢出。开局面板可操作，六波进度折为编号。
- 窄屏 4× 速度走完演示局，胜利结算显示 73 击杀、179 秒、360 次助威。结算面板高 255，战场容器高 410，不再裁切标题、贡献或重开按钮。
- 本轮浏览器的 warning/error 日志为空。此为浏览器缩窄模拟，未在手机真机验证。

## 浏览器实际试玩

- 开发版首屏加载，三路画布、城墙、炮塔、怪物正常显示。
- 开始演示后加入第四座火炮，界面显示个人昵称及炮位 4/12。
- 实际点击中路换防成功，立即再点击下路提示五秒冷却，炮塔仍留在中路。
- 暂停后倒计时固定，助威被拒绝；继续后战斗恢复。
- 使用二倍速、四倍速，完整走到胜利结算：73 击杀、179 秒、360 次全体助威，两次全队增伤。
- 查询浏览器运行日志，没有记录到 warning 或 error。
- 正式构建包由 tools/serve.mjs 提供，首页 HTTP 200，页面加载并开始战斗。
- 准备阶段先加入自己的炮塔，再开始游戏，倒计时内个人炮塔仍存在（4/12），没有被重置。
- 390 × 844 窄屏检查：页面宽度 375（另有滚动条），没有横向溢出，画布一个，战场与面板改为上下排列。仅是桌面浏览器缩窄模拟，不是手机真机测试。

## 浏览器实时压测（2026-09-20 最终复测）

正式构建包、1440 × 1000 窗口、1× 速度，三位演示队员开局。开始压测后自动观众暂停，200 个身份每秒 50 条事件，持续 120 秒。

- 实际注入：6,000 条。
- 完成并恢复自动观众后的采样：接收 6,035、入队 6,035、处理 6,035、排队 0（总数包含开局和恢复后的正常演示事件）。
- 队列/身份溢出 0、无效输入 0、限流 0、旧局 0；动作冷却拒绝 6，属于压测接管已有观众时的正常三秒冷却。
- 自动观众开关已恢复为开，后续模拟弹幕继续进入。
- 此次采样界面 FPS 约 209—235；该数字是本机瞬时平滑估计，不是最低帧率保证或跨设备性能结论。
- 两次升级均正常生效。
- 压测后继续到第六波并正常结算：本轮因 180 秒时剩余两只敌人而超时失败（71 击杀），失败界面与贡献榜正常；最终接收/入队/处理均为 6,128，排队 0、溢出 0，浏览器 warning/error 记录为空。压力测试不是胜率测试，注入大量助威会改变雷暴触发时机；默认未压测演示的取胜结果另见上表。

此前一轮实时压测发现三名尚未发言的演示观众在恢复时被容量限制拒绝，已通过预留身份修复；上面的结果是修复后的复测，不隐藏此前失败。

## 构建

`node node_modules/vite/bin/vite.js build`：通过，生成 dist/。v0.3 的 Phaser 单独分包约 1.21 MB（gzip 约 332 KB）；其他 JS 约 49 KB，CSS 约 31 KB。以实际构建输出为准。

另实际执行 package.json 中的构建脚本：`pnpm --config.verify-deps-before-run=warn run build`，通过类型检查和构建。本机首次安装后才增加 allowBuilds，依赖状态检查会给出配置不同的警告；标准安装配置已随源码保存，不把该警告描述为构建失败。

Windows 启动入口：实际执行 `启动游戏.cmd`，在测试端口 4174 启动成功，访问首页返回 HTTP 200 且包含游戏标题。最初脚本的 UTF-8 中文/LF 组合在 cmd 解析时出错，已改为 ASCII 提示和 CRLF，并在修改后完成上述复测。测试用服务随后停止，正常预览保留 4173。

## 尚未验证

真实抖音接口、付费礼物、手机真机、跨浏览器长期稳定性、网络直播端到端延迟、音频输出设备实际听感、长时间内存曲线均未验证。
